# Copy to .env and adjust as needed. Any SQLAlchemy-supported URL works.
#
# Postgres (recommended / default deployment target):
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/sdip
#
# SQLite fallback (local file-based, no DB service needed):
# DATABASE_URL=sqlite:///./waitlist.db
